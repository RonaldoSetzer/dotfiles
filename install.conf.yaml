- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.config/bat:
      create: true
      path: config/bat
    ~/.config/alacritty:
      create: true
      path: config/alacritty
    ~/.config/nvim:
      create: true
      path: ../neovim-setup

- create:
    - ~/Workspace

- shell:
  - 
    command: ./setup_fonts.zsh
    stdout: true
    stderr: true
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
